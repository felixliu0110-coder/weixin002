# 我形我衣 V22：V1 前端 UI / 交互重设计

## 本次目标
不是改业务逻辑，而是在 V21 已完成的用户流程基础上，把“能用”继续整理为“像一个正式产品”：
- 建立统一视觉层级、间距、圆角、按钮体系。
- 把关键文字操作改成图形 + 文字，减少“纯文字像链接”的廉价感。
- 主操作只有一个视觉焦点，次操作降级，危险操作明确。
- 统一卡片、输入框、筛选项、底部操作区。
- 衣橱/选择衣物/结果页优先展示图片，文字负责解释，不抢主视觉。
- 不改变 V1 试穿后端、Provider、额度、任务状态等业务逻辑。

## 主要调整
1. 全局 Design Tokens：降低过大的圆角和过重阴影，统一米白 + 暖棕色调。
2. `btn`：主按钮改为更稳重的矩形圆角按钮；secondary / danger / dark 层级清晰。
3. `nav-bar`：缩小返回按钮视觉重量，标题居中并统一左右安全区。
4. `tabbar`：降低高度与图标重量，减少底部视觉噪音。
5. 首页：搜索、核心试穿入口、额度、衣物、人物按“主任务 → 状态 → 内容 → 人物”排序。
6. 衣橱：分类变成轻量筛选条；新增按钮图形化；衣物图片优先；空状态更明确。
7. 选择衣物：选中态、上传入口、底部主 CTA 分层；上传只保存，不自动试穿。
8. 生成页：状态卡片更轻，失败操作保留“重新生成 / 返回重选”。
9. 结果页：收藏 / 再试一次 / 分享改成三项操作卡，不再堆成普通文字按钮。
10. 人物页：真实人物照片作为主视觉，身体数据作为规整数据卡。
11. 添加衣物 / 衣物详情：照片、信息、编辑、试穿、删除按优先级排列。
12. 账号 / 反馈 / 收藏 / 历史：统一列表与空状态视觉。
13. 登录页：移除主 TabBar，避免把 onboarding 误认为已进入主应用。
14. 关键 CTA 增加图标；图标仅辅助含义，不替代中文文字。

## 交互原则
- 点击区域至少保持足够触控尺寸。
- 不使用隐藏的“文字链接”承担主要动作。
- 长按仍只作为快捷动作，不承担唯一入口。
- 删除始终使用危险色并与普通操作拉开层级。
- 上传衣物永远是“保存衣物”，试穿必须由用户主动点击。
- 试穿结果页的分享继续使用微信原生分享能力。

## 验证
- 全部 JS 文件 `node --check`：0 错误。
- 未改变云函数 / AI Provider / 额度核心实现。
- V22 目标是 UI / UX 重设计，不消耗额外 AI 额度。
