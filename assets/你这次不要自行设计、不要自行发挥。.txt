你这次不要自行设计、不要自行发挥。

ChatGPT 已经完成仓库检查。

你的唯一职责：
【把 ChatGPT 明确给出的修改原样上传到 GitHub】

仓库：
https://github.com/felixliu0110-coder/weixin002

基准：
main
a2b947f454c10ee3b065e808ce54c82748da9f4d

==================================================
一、绝对禁止
==================================================

不要：

1. 自行重构
2. 自行增加 AI Provider
3. 自行修改数据库结构
4. 自行增加 FitEngine
5. 自行增加 BodyProfile 数据表
6. 自行修改缓存协议
7. 自行删除三视图
8. 自行删除四视图
9. 自行改变生产 Provider
10. 自行增加 npm 依赖
11. 自行增加服务器/GPU/Redis/MQ
12. 自行修改 UI
13. 自行修改产品流程
14. 自行“优化” ChatGPT 没有要求的代码

如果发现 ChatGPT 没有明确说明的潜在问题：

只报告，不修改。

==================================================
二、这次任务范围
==================================================

只处理：

P0 真实代码问题
+
最小成本链路修复
+
回归测试
+
PRD/架构文档同步

不要进行 P1 大规模架构重构。

==================================================
三、P0-1：aiTryon 视频额度顺序
==================================================

文件：

cloudfunctions/aiTryon/index.js

当前必须修复：

视频模式不能在 imageTaskId 完整验证之前调用 consumeQuota()。

视频必须严格：

1. 用户身份验证
2. avatarViewId 验证
3. garmentIds 验证
4. 资源 ownership 验证
5. imageTaskId 存在性验证
6. imageTaskId 属于当前用户
7. imageTaskId 类型为 ai_image
8. imageTaskId status=success
9. tryon_image_url 存在且有效
10. cache / idempotency 检查
11. quota 检查
12. consumeQuota()
13. 创建视频任务
14. Provider 调用

任何 1-10 步失败：

不得扣用户额度。

==================================================
四、P0-2：appError
==================================================

检查：

cloudfunctions/aiTryon/index.js

当前代码使用了 appError()。

确认顶部是否正确：

const { fmtErr, appError } = require("../services/errors");

如果当前代码确实缺少 appError：

只补这个 import。

不要修改 errors.js。

不要重新设计错误系统。

==================================================
五、P0-3：quota
==================================================

文件：

cloudfunctions/services/quota.js

不要重写。

保留当前事务机制。

重点检查：

consumeQuota()
refundQuota()

确认：

A. 输入验证失败不扣额度
B. cache hit 不扣额度
C. duplicate request 不重复扣额度
D. Provider failure 有明确 refund
E. refund 不会重复执行
F. 并发请求不会超扣

如果当前代码已经满足：

不要修改。

如果发现明确 bug：

只修复明确 bug。

==================================================
六、P0-4：ensureGarmentViews
==================================================

当前前端：

miniprogram/pages/image-preview/index.js

正式试穿前存在：

api.ensureGarmentViews(...)

当前 aiTryon 真正生成图片时已经主要使用：

人物 composite
+
衣物 originalFileId

因此：

正式图片试穿链路不应为了生成暂未验证有收益的四视图而额外产生一次 AI 调用。

修改：

image-preview/index.js

删除正式提交试穿前的：

api.ensureGarmentViews(...)

让流程直接：

requestSubscribe()
↓
api.submitAiTryon(...)

但是：

禁止删除：

cloudfunctions/ensureGarmentViews

禁止删除：

miniprogram/utils/api.js 中对应接口

禁止删除相关数据库/存储结构。

原因：

四视图仍然保留为未来 POC 能力。

==================================================
七、三视图
==================================================

不要删除：

createAvatarViews

不要修改三视图生成代码。

只在文档中调整定位：

三视图是可选人物资产能力。

目前没有经过对照 POC，因此不能写：

“三视图一定提高试穿质量”。

==================================================
八、缓存
==================================================

不要修改当前 cache key。

原因：

当前 avatarViewId 已经承担人物资产版本作用。

没有 POC 证据证明现在必须引入：

modelVersion
promptVersion
normalizationVersion

所以本次不改缓存协议。

==================================================
九、不要增加这些文件
==================================================

本次不要新增：

bodyProfile.js
fitEngine.js
garmentProfile.js
tryonPlanner.js
qualityGate.js

这些属于下一阶段设计。

目前只写入架构候选文档，不进入生产代码。

==================================================
十、Provider
==================================================

不要接入腾讯/阿里生产。

不要修改当前生产 Provider。

不要自动 fallback。

不要增加多个付费 Provider。

后续 Provider 需要单独进行 POC。

==================================================
十一、测试
==================================================

检查现有测试结构。

至少增加/修改测试覆盖：

1. 视频 imageTaskId 不存在
   → 不扣额度

2. imageTaskId 不属于当前用户
   → 不扣额度

3. imageTaskId 类型错误
   → 不扣额度

4. imageTaskId 未成功
   → 不扣额度

5. tryon_image_url 不存在
   → 不扣额度

6. cache hit
   → 不扣额度

7. 正常 Provider 成功
   → 正常扣额度

8. Provider failure
   → 按当前 refund 规则处理

9. duplicate request
   → 不重复扣额度

不要为了测试增加新的测试框架。

==================================================
十二、文档
==================================================

更新：

docs/PRD-我形我衣-v1.0.md
docs/architecture.md

新增：

docs/architecture-v2-proposed.md

文档必须明确：

1. V1 核心是图片试穿。
2. 视频属于后续 POC。
3. 三视图不是已验证的强制前置条件。
4. 四视图不是已验证的强制前置条件。
5. 生产只启用一家付费 AI Provider。
6. 备用 Provider 必须经过 POC。
7. 不新增 GPU。
8. 不新增复杂 AI 基础设施。
9. 确定性业务逻辑优先自己实现。
10. 未经 POC 的模型、算法、Provider 不得写成生产能力。
11. 未经成本验证不得声称成本可控。
12. 未经许可证确认不得声称可以商业使用。

==================================================
十三、architecture-v2-proposed.md
==================================================

请严格使用 ChatGPT 提供的最终内容。

不要自己重新写。

如果 ChatGPT 没有提供某段内容：

不要擅自补充技术方案。

==================================================
十四、Git
==================================================

修改前：

git status
git log -1

确认基准。

修改后：

git diff
git status

检查：

1. 是否有无关文件
2. 是否修改 package.json
3. 是否新增依赖
4. 是否泄露 secret
5. 是否修改 Provider
6. 是否修改数据库结构
7. 是否删除三视图
8. 是否删除四视图

如果出现以上任何非预期修改：

停止。

不要提交。

==================================================
十五、测试
==================================================

执行仓库现有 CI/test。

不要伪造结果。

测试不能运行：

明确告诉 ChatGPT：

“无法运行，原因：……”

不能说：

“测试通过”。

==================================================
十六、提交
==================================================

如果所有修改都符合上述范围：

提交。

建议 commit：

fix: harden tryon quota validation

如果文档单独提交：

docs: align tryon architecture with validated scope

如果多个 commit 更清晰，可以拆分。

==================================================
十七、最终报告
==================================================

只报告真实结果：

1. 修改文件
2. 每个文件实际修改内容
3. 删除内容
4. 新增内容
5. 测试结果
6. commit SHA
7. main 最新 SHA
8. 是否 push 成功
9. 如果失败，完整说明原因

禁止说：

“应该可以”
“理论上可以”
“已经验证”

除非确实执行过。

==================================================
十八、最重要
==================================================

如果你发现 ChatGPT 指定内容和当前仓库实际代码不一致：

不要自行猜。

先停止修改，并把：

文件
当前代码
冲突点

报告给 ChatGPT。

你只负责：

【上传已经确定的修改】

不负责：

【设计方案】
【自行判断】
【自行扩展】