第三轮技术辩论：十二个争议逐条回应


搜索结果出来了——ChatGPT 第一条质疑直接命中。
商业 Multi-garment VTON API 不仅存在，而且不止一家。
这一轮的格局和前两轮不同：有真正需要反驳的点，也有必须认错的点。

争议 1："目前不存在成熟商业 Multi-garment VTON API"——依据是什么？
搜索结果
我错了。这句话是事实性错误。
市场搜索发现至少两个商业 VTON API 明确支持上装+下装组合试穿：
PixelAPI — Outfit Try-On API
• 端点：POST /v1/virtual-tryon/outfit
• 上线时间：2026-07-13
• 输入：person_image + upperbody_garment + lowerbody_garment
• 内部流程：先下半身 VTON → 再上半身 VTON → 返回合成结果
• 定价：22 credits（单件 11 credits 的 2 倍）
• 耗时：~90s（标准）/ ~30-45s（SCALE 套餐）
• 内置防布料渗透：bottom-first 顺序不可配置，防止上装颜色渗入下装区域
• 失败处理：Stage 1 失败全额退款；Stage 2 失败退 Stage 2 费用并返回 Stage 1 的下装结果
• 内置质量门控：NSFW 检测、人物构图检查、服装有效性验证、品类匹配检测
FitRoom — combo 模式
• cloth_type: "combo" — 上装 + 下装一次请求
• 定价：1 credit（与单件相同！）
• 另有 full_set 支持连体服/连衣裙
• 内置模特检测端点：返回 good_clothes_types: ["upper", "lower", "full"]，基于人物可见区域判断可支持的服装类别
• 内置服装检测端点：自动识别服装类型 + 有效性验证
• HD 模式：最高 2048px 输出
• 2026-08-07 新增多角度试穿（正/侧/背）
FASHN.ai
• 自助 API，支持 upper / lower / dress
• $0.075 按需，$0.049 批量
• 未明确提及组合试穿
我为什么错了
我说"不存在"时，依据是"我的行业认知"。但行业认知不等同于事实——我没有实际检索市场就做了断言。这和"15% 阈值没有数据就写进方案"是同一类错误：用未经验证的假设充当事实。
修正
旧表述（删除）：
  "目前不存在成熟商业 Multi-garment VTON API"

新表述：
  "当前已验证至少存在两个商业 VTON API 支持上装+下装组合试穿
   （PixelAPI Outfit 端点、FitRoom combo 模式），
   但其输出质量、稳定性、商业授权、数据隐私等是否符合本项目要求，
   需要通过 Provider 调研 + POC 确认。"

这对 V1 架构的影响
之前的方案把"级联 VTON"列为 V1 候选，因为担心自己实现级联的误差叠加。现在有了 Provider 原生支持的 Outfit 端点：
旧方案：
  自己实现级联 VTON → 误差叠加风险 → V1 候选，需 POC

新方案：
  使用 Provider 的 Outfit 端点 → Provider 内部处理级联 → 我们只需一次 API 调用
  → 仍然是 V1 候选（需 POC 验证输出质量），但技术风险大幅降低

判定：ChatGPT 胜。我的事实性断言错误。修正表述，并将 Outfit 端点纳入 POC 候选 Provider 列表。

争议 2：人物质量检测 ≠ MediaPipe 锁死
ChatGPT 的核心论点
人物质量检测是产品能力（V1 确定），MediaPipe 是技术实现（不应锁死）。
我的回答：**你对了。能力与实现必须分离。**
但我要补充一个 ChatGPT 没考虑的工程约束：微信小程序前端的检测工具选项非常有限。
检测方案	微信小程序可用性	优劣
MediaPipe (WASM)	⚠️ 需要验证 WebView 兼容性	成熟、轻量、关键点检测准确
TensorFlow.js	✅ 小程序可运行	比 MediaPipe 重，模型加载慢
OpenCV.js	✅ 可运行	主要做图像处理，人体检测需额外模型
云端 Vision API	✅ 可调用	增加网络延迟 + API 成本，离线不可用
Provider 自带检测	✅ PixelAPI/FitRoom 都有	最准确但有 API 成本，且绑定 Provider

最实际的方案是：前端用轻量检测做快速筛选（MediaPipe 或 TF.js），后端用 Provider 的检测端点做精确验证。
修正方案
人物质量检测 — 能力与实现分离

【V1 已确定：产品能力】
  ├── 硬条件检测：单人 / 身体完整度 / 分辨率
  ├── 软评分检测：身体朝向 / 人脸质量 / 清晰度 / 光照
  └── 按服装类别的可用区域判断（见争议 4）

【V1 候选：技术实现】
  ├── 前端轻量检测（候选：MediaPipe / TF.js / OpenCV.js）
  │   → 做快速筛选，阻止明显不合格的输入
  │   → 具体用哪个前端方案，POC 阶段根据兼容性和性能决定
  │
  └── 后端 Provider 检测（候选：PixelAPI Check Model / FitRoom 模特检测器）
      → 做精确验证，返回可支持的服装类别
      → 检测结果更可靠但有 API 成本和延迟

判定：ChatGPT 胜。能力与实现分离。MediaPipe 是候选实现，不是锁死选择。

争议 3：重新检查所有数字阈值
逐个分类：
数字	分类	依据
512px（人物短边最低分辨率）	A：模型/API 要求	PixelAPI 文档明确要求 768×1024px 或更高；FitRoom 接受任意分辨率但低分辨率影响输出质量。512px 是 VITON-HD 模型的标准训练分辨率下限。但具体阈值应跟随所选 Provider 的文档要求
30°（人脸偏航角）	B：临时工程参数	无数据支撑。改为"人脸偏航角较大时提示可能影响效果，具体角度待 POC 确定"
45°（身体偏转角）	B：临时工程参数	同上。改为"身体偏转较大时提示，具体角度待 POC"
50%（关键点可见率 — 遮挡判断）	B：临时工程参数	无数据支撑。改为"遮挡程度评分，阈值待 POC"
80%（关键点可见率 — 良好评级）	B：临时工程参数	同上
30 秒（生成等待时间提示）	B：临时工程参数	基于 PixelAPI ~12-18s / FitRoom ~9s 的推理时间估算，加缓冲。实际跟随 Provider
10/20/100 张（POC 样本量）	B：临时工程参数	POC 阶段的探索性样本量，不是正式统计要求。可调整
2 人标注	B：临时工程参数	上轮已讨论，2 人足够产品 POC

修正方案
所有数字阈值的处理规则：

1. 来源为 Provider 官方文档的 → 引用文档，标明来源
2. 来源为工程经验的 → 标注"临时工程参数，POC 后调整"
3. 无任何依据的 → 删除具体数字，改为定性描述

修正后的阈值表述：

  分辨率：
    → "人物图片分辨率应满足所选 Provider 的文档要求
       （PixelAPI 推荐 768×1024px+，FitRoom 无硬性最低要求）"
    → 不再写"≥ 512px"作为通用标准

  人脸角度：
    → "人脸严重偏转时提示用户可能影响效果"
    → 不再写"偏航角 < 30°"

  身体偏转：
    → "身体偏转较大时提示用户"
    → 不再写"< 45°"

  遮挡程度：
    → "遮挡严重时提示，轻微遮挡允许继续"
    → 不再写"可见率 > 80%"

判定：ChatGPT 胜。大部分数字降级为临时工程参数。Provider 文档有要求的引用文档，其余删除具体数字改为定性描述。

争议 4：人物完整性应按衣物类别判断
ChatGPT 的核心论点
上装需要肩/胸/腰/上臂，下装需要腰/臀/腿，连体服需要全身。不能用统一规则。
我的回答：**完全正确。而且搜索结果直接验证了这个设计。**
FitRoom 的模特检测端点返回的正是这个逻辑：
// json
{
  "is_good": true,
  "good_clothes_types": ["upper", "lower", "full"]
}

一个半身照可能返回 ["upper"]——只能试穿上装。一个全身照返回 ["upper", "lower", "full"]。
修正方案
人物完整性判断 — 按服装类别分区

通用硬条件（不区分服装类别）：
  ├── 单人
  ├── 头部可见（身份锚定需要）
  └── 分辨率满足 Provider 要求

按服装类别的可用区域判断：

  上装（upper）：
    需要：肩部 + 胸部 + 腰部 + 至少一侧上臂
    半身照可以 → ✅ 可试穿上装

  下装（lower）：
    需要：腰部 + 臀部 + 大腿 + 至少一侧小腿
    半身照不行 → ❌ 需全身照

  连体服（full_set / dress）：
    需要：全身（肩到脚）
    半身照不行 → ❌ 需全身照

实现方式：
  ├── 前端：关键点可见性粗判断
  │   → 肩/肘关键点可见 → 可试穿上装
  │   → 髋/膝关键点可见 → 可试穿下装
  │
  └── 后端：Provider 检测端点精确判断
      → FitRoom: good_clothes_types
      → PixelAPI: Person framing check

判定：ChatGPT 胜。人物完整性按服装类别分区判断，不再使用统一规则。

争议 5：脸不可见能不能继续？
我的回答：**技术上可以，产品上需要标记。**
ChatGPT 对了：产品价值是"看到自己穿上衣服的效果"，如果看不到脸，"自己"的成分就打折了。
修正方案
人脸可见性分级处理：

  ✅ 人脸清晰可见 → 正常流程，最高身份可信度

  ⚠️ 人脸部分可见（侧脸/遮挡/模糊）
    → 允许继续 VTON
    → 结果页标注"面部参考不足，身份一致性可能较低"
    → 建议用户上传面部更清晰的照片

  ❌ 人脸完全不可见
    → 允许继续 VTON（技术上可行）
    → 结果页标注"未检测到面部，无法保证与您本人一致"
    → 强烈建议用户上传包含面部的照片
    → 不阻止用户继续（因为有些人可能接受无脸试穿效果）

判定：ChatGPT 胜。人脸可见性不影响 VTON 执行，但影响产品层的身份可信度标注。

争议 6：Provider Success ≠ 结果 Success
我的回答：**完全同意。这是必须修改的架构问题。**
修正后的任务状态设计
Try-On Task 状态机：

  SUBMITTED        → 任务已提交
  RUNNING           → Provider 正在处理
  PROVIDER_SUCCESS  → Provider 返回了图片（执行成功）
  PROVIDER_FAILED   → Provider 处理失败（API 错误/超时/NSFW 拒绝等）

  从 PROVIDER_SUCCESS 进入质量检查：
  QUALITY_CHECKING  → 正在自动质量检查
  QUALITY_PASS      → 自动检查通过
  QUALITY_WARNING   → 自动检查发现轻微问题（可展示但需标注）
  QUALITY_FAILED    → 自动检查发现严重问题（不应展示）

状态流转：
  SUBMITTED → RUNNING → PROVIDER_FAILED（终止，返回错误信息）
  SUBMITTED → RUNNING → PROVIDER_SUCCESS → QUALITY_CHECKING → QUALITY_PASS
                                                           → QUALITY_WARNING
                                                           → QUALITY_FAILED

V1 的自动质量检查项（最小集合）：
#	检查项	方法	失败标准
1	Provider 是否返回了有效图片	HTTP 状态 + 图片格式检查	无图/非图片格式
2	图片中是否存在人物	人物检测	无人物
3	人物是否严重缺失	关键点检测	头部或主要躯干缺失
4	输出分辨率是否合理	图片尺寸	与输入差异 > 50%
5	是否出现明显黑色/纯色块	像素分布分析	大面积纯色区域
6	任务耗时记录	时间戳	记录但不判定

V1 不做的质量检查：
#	检查项	原因
身份一致性	需要人脸比对，V1 不做	
服装保真度	需要服装特征比对，V1 不做	
穿着自然度	需要人工判断或复杂 CV	
身体比例	无数据支撑，V1 不做	

判定：ChatGPT 胜。执行状态和质量状态必须分离。V1 做最小自动质量检查，复杂质量评估通过 POC + 用户反馈积累。

争议 7：生成结果必须有最基本的质量检查
这和争议 6 一起回答了。V1 的最小质量评估体系就是上面的 6 项检查。
判定：ChatGPT 胜。V1 最小质量检查 = 6 项自动检测 + 人工 POC 验证 + 用户反馈。

争议 8：AI 人物锚定图命名和定位
我的回答：**ChatGPT 对了。"锚定图"暗示"这是用户的锚定/参考"，但它实际上是基于参数的估算。**
修正
旧术语（删除）：
  "AI 人物锚定图" — 暗示这是用户的锚定/锚点

新术语：
  "AI 估算形象" — 准确描述：基于参数估算的、非真实的人物形象
  内部代码变量名：estimated_avatar（非 anchor_image）

产品层面的透明度：
  当用户进入 AI 估算形象路径时：
  → 明确提示："您未上传照片，系统将根据您提供的身材数据估算一个参考形象"
  → "估算形象可能与您的真实外观存在差异，建议上传照片获得更准确的试穿效果"
  → 结果页标注"基于估算形象的试穿效果"

判定：ChatGPT 胜。重命名为"AI 估算形象"，代码变量同步修改。

争议 9：AI 估算形象的内部组件不应独立为 V1 已确定
我的回答：**逻辑正确。子组件的状态不能高于父路径的状态。**
AI 估算形象路径 = V1 候选

其内部组件的状态：
  ├── 参数翻译引擎 → V1 候选（跟随父路径）
  ├── BMI 粗分类 → V1 候选（跟随父路径）
  ├── Negative prompt → V1 候选（跟随父路径）
  ├── 摄影术语 prompt → V1 候选（跟随父路径）
  ├── 合成三视图预览 → V1 候选（跟随父路径）
  └── 体型约束优先级 → V1 候选（跟随父路径）

只有当 POC 证明 AI 估算形象路径值得进入 V1，
这些子组件才正式进入 V1。

但有一个例外：
  Negative prompt 和摄影术语是通用的 AI 生图优化手段，
  不只服务于 AI 估算形象路径。
  如果 V1 有其他 AI 生图调用（如服装背面图），
  这些优化手段仍然有用。
  → Negative prompt + 摄影术语 = V1 候选（独立于 AI 估算形象路径，
    因为它们可能在其他场景也使用）

判定：ChatGPT 胜。AI 估算形象路径的子组件跟随父路径状态。独立通用组件单独标注。

争议 10：单件 VTON 架构确定 ≠ Provider 确定
我的回答：**完全同意。架构决策和实现验证是两件事。**
架构层（V1 已确定）：
  ✅ 使用 VTON 做虚拟试穿
  ✅ 单件 VTON 是核心能力（1 上装 或 1 下装）
  ✅ VTON 通过抽象接口 + 适配器模式调用
  ✅ VTON 调用是异步模式

Provider 层（必须 POC）：
  ⚠️ 具体 Provider 的输出质量
  ⚠️ 具体 Provider 的推理延迟
  ⚠️ 具体 Provider 的稳定性/可用性
  ⚠️ 具体 Provider 的商业授权
  ⚠️ 具体 Provider 的数据隐私
  ⚠️ 具体 Provider 的输入要求
  ⚠️ 具体 Provider 的错误处理

判定：ChatGPT 胜。架构确定和 Provider 验证必须分离。

争议 11：重新分类所有项目
#	项目	V1 已确定	V1 候选/POC	暂不做	理由
1	真实用户照片 → VTON	✅			逻辑确定性
2	人物质量检测（能力）	✅			V1 必须存在
3	MediaPipe（实现）		⚠️		候选实现之一
4	单件 VTON（架构）	✅			VTON 核心设计用例
5	VTON Provider（具体选择）		⚠️		必须通过 POC 验证
6	AI 估算形象（整体路径）		⚠️		需 POC 验证必要性
7	AI 估算形象（内部组件）		⚠️		跟随父路径状态
8	上装 VTON（架构）	✅			VTON 核心能力
9	下装 VTON（架构）	✅			VTON 核心能力
10	连续两次 VTON（自实现）			❌	有 Provider 原生 Outfit 端点替代
11	Outfit 端点（Provider 原生）		⚠️		已验证存在，质量需 POC
12	多衣物一次 VTON（>2件）			❌	无 Provider 支持
13	三视图		⚠️		仅 AI 估算形象路径，跟随父路径
14	服装四视图			❌	无使用场景
15	视频			❌	V1 不可控
16	BMI 粗分类		⚠️		仅 AI 估算形象路径，低优先级
17	OpenPose			❌	V1 无使用场景
18	SMPL / SMPL-X			❌	V2+
19	自动抠图		⚠️		方向可行但可能损害 VTON，需 POC
20	多 Provider Adapter	✅			软件架构，无需 POC
21	Provider 执行状态	✅			架构必须
22	视觉质量状态	✅			架构必须（与执行状态分离）
23	最小自动质量检查（6项）	✅			V1 必须存在
24	按服装类别的人物区域判断	✅			逻辑正确，FitRoom 已验证此设计
25	人脸可见性分级标注	✅			产品层必须
26	Negative prompt + 摄影术语		⚠️		通用 AI 生图优化，候选但非独立 V1 已确定
27	Provider 内置检测端点		⚠️		候选但绑定 Provider 选择


争议 12：完整 V1 AI 穿搭流程
═══════════════════════════════════════════════════════
V1 AI 穿搭生成完整流程（第六次修订）
═══════════════════════════════════════════════════════

【用户输入】
  ├── 人物照片（推荐，可选）
  ├── 身体参数（身高/体重/围度，可选）
  └── 服装照片 + 品类标签

        ↓

【人物输入处理】[V1 已确定：能力][V1 候选：实现细节]

  用户上传了照片？
  │
  ├── 是 → 人物质量检测
  │       │
  │       ├── 硬条件检测 [V1 已确定]
  │       │   ├── 单人？（人物检测）
  │       │   ├── 分辨率满足 Provider 要求？
  │       │   └── 身体严重缺失？
  │       │       → 不通过：要求重新上传
  │       │
  │       ├── 软评分检测 [V1 候选]
  │       │   ├── 身体朝向
  │       │   ├── 人脸质量
  │       │   ├── 清晰度
  │       │   ├── 光照
  │       │   └── 遮挡程度
  │       │       → 低分：提示用户但允许继续
  │       │
  │       └── 按服装类别的可用区域判断 [V1 已确定]
  │           ├── 上装：肩+胸+腰+上臂可见 → 可试穿上装
  │           ├── 下装：腰+臀+腿可见 → 可试穿下装
  │           └── 连体服：全身可见 → 可试穿
  │               → 不可用区域：提示用户"该照片不支持此类别试穿"
  │
  └── 否 → AI 估算形象降级路径 [V1 候选]
          ├── 参数翻译引擎
          ├── 体型约束（真实测量值 > BMI > 无数据）
          ├── Negative prompt + 摄影术语
          ├── 生成高清正面全身图（1 次 AI 调用）
          ├── 可选：合成三视图预览
          ├── 明确标注"AI 估算形象"
          └── 后台记录关键点比例数据（纯日志）

        ↓

【照片预处理】[V1 候选]

  可自动修复的问题（POC 验证后决定）：
  ├── 亮度归一化 → 简单可靠，V1 候选
  ├── 旋转校正 → 简单可靠，V1 候选
  └── 抠图/超分 → 需 POC，可能损害 VTON

  不可修复的问题 → 要求重新上传

        ↓

【衣物输入处理】[V1 已确定]

  用户上传服装原图 + 选择/自动检测品类
  → 不做额外生成

        ↓

【穿搭编排】[V1 已确定：架构][V1 候选：Outfit]

  ├── 单件：1 上装 或 1 下装 [V1 已确定]
  │   → 调用 Provider 单件 VTON 端点
  │
  └── 双件：1 上装 + 1 下装 [V1 候选]
      ├── 优先使用 Provider 原生 Outfit 端点
      │   （PixelAPI /outfit 或 FitRoom combo）
      └── 如 Provider 不支持 Outfit → 不做双件（V1 不自实现级联）

        ↓

【VTON 调用】[V1 已确定：架构][V1 候选：Provider]

  通过 VTON 抽象接口调用
  ├── 业务层：tryOn(personImage, garmentImage, category)
  │   扩展：tryOnOutfit(personImage, upperGarment, lowerGarment)
  └── 适配器层：支持 mask/metadata/providerOptions

  异步模式：submit → poll

        ↓

【执行状态检查】[V1 已确定]

  PROVIDER_FAILED → 返回错误信息
  PROVIDER_SUCCESS → 进入质量检查

        ↓

【视觉质量检查】[V1 已确定]

  最小 6 项自动检查：
  ├── 有效图片？ → 否 → QUALITY_FAILED
  ├── 人物存在？ → 否 → QUALITY_FAILED
  ├── 人物严重缺失？ → 是 → QUALITY_FAILED
  ├── 分辨率合理？ → 否 → QUALITY_FAILED
  ├── 明显色块/错误？ → 是 → QUALITY_FAILED
  └── 通过 → QUALITY_PASS

  人脸可见性分级标注：
  ├── 清晰可见 → 无标注
  ├── 部分可见 → "身份一致性可能较低"
  └── 不可见 → "无法保证与您本人一致"

        ↓

【结果展示】[V1 已确定]

  1 张试穿图
  ├── 默认白底
  ├── 保存/分享/收藏
  ├── AI 生成标识
  ├── 质量标注（如有 WARNING）
  ├── 身份可信度标注（人脸不可见时）
  └── 隐藏"简约室内"实验开关 [V1 候选]


第三轮裁决

A：这一轮 ChatGPT 错在哪里
#	ChatGPT 的观点	我的反驳
无	—	—

这一轮 ChatGPT 没有事实性错误。 每条质疑都指向了我方案中的真实问题。我找不到可以反驳的技术论点。

B：这一轮我错在哪里
#	我的错误	修正
B1	"不存在成熟商业 Multi-garment VTON API"	事实性错误。至少 PixelAPI 和 FitRoom 支持 Outfit 端点
B2	MediaPipe 和人物质量检测绑定	能力与实现分离，MediaPipe 是候选不是锁死
B3	数字阈值（30°/45°/80%等）无分类	大部分降级为临时工程参数，Provider 有要求的引用文档
B4	人物完整性用统一规则	改为按服装类别分区判断
B5	脸不可见无分级处理	技术不阻止但产品层做身份可信度标注
B6	Provider Success = 结果 Success	拆分为执行状态 + 视觉质量状态
B7	生成结果无基本质量检查	增加最小 6 项自动检查
B8	"AI 人物锚定图"命名	重命名为"AI 估算形象"
B9	AI 估算形象子组件独立标记为 V1 确定	子组件跟随父路径状态
B10	单件 VTON 架构确定 = Provider 确定	架构和 Provider 验证分离


C：双方已经确定的事实
1. V1 核心是单件 VTON + 真实照片 — 最高优先级
2. 商业 VTON API 存在 Outfit 端点 — 上装+下装可一次调用
3. 能力与实现必须分离 — 质量检测是能力，具体工具是实现
4. 数字阈值必须有来源分类 — A(官方要求)/B(临时参数)/C(正式标准)
5. 人物完整性按服装类别判断 — 上装/下装/连体服不同要求
6. Provider Success ≠ Visual Quality Success — 两个维度独立评估
7. AI 估算形象不是用户本人 — 术语和标注必须透明
8. 架构确定 ≠ Provider 确定 — 两层分离
9. V1 最小质量检查必须存在 — 6 项自动检测
10. 级联 VTON 自实现不应进入 V1 — 优先使用 Provider Outfit 端点

D：仍然无法理论确定的问题
#	问题
D1	哪个 VTON Provider 综合最优（需 POC）
D2	Provider Outfit 端点的输出质量是否满足产品要求（需 POC）
D3	自动抠图是否损害 VTON 质量（需 POC）
D4	前端检测方案在微信小程序中的兼容性（需实测）
D5	AI 估算形象的产品价值是否值得开发投入（需用户数据）
D6	有多少比例用户需要 AI 估算形象降级路径（需上线后数据）


E：必须进行 POC 的问题
POC	目标	优先级
POC-01	VTON Provider 对比（PixelAPI / FitRoom / FASHN.ai / 其他）	🔴 最高
POC-02	单件 VTON 输出质量验证	🔴 最高
POC-03	Provider Outfit 端点质量验证	🔴 高
POC-04	真实照片 vs AI 估算形象的 VTON 输出差异	🟡 中
POC-05	自动抠图 vs 直接送 VTON 的质量对比	🟡 中
POC-06	前端检测方案在微信小程序中的兼容性	🟡 中
POC-07	白底 vs 简约室内用户感知	🟢 低


F：最终 V1 架构
═══════════════════════════════════════
V1 已确定（可直接实施）
═══════════════════════════════════════

  ✅ VTON 抽象接口 + 分层适配器（业务层 + Provider 层）
  ✅ 真实照片 → 人物质量检测 → 合格直接 VTON
  ✅ 人物质量检测（硬/软分层 + 按服装类别区域判断）
  ✅ 单件 VTON（1 上装 或 1 下装）— 架构确定
  ✅ 服装原图直接作为 VTON 输入
  ✅ 执行状态 + 视觉质量状态 分离
  ✅ 最小 6 项自动质量检查
  ✅ 人脸可见性分级标注
  ✅ 默认白底 + AI 生成标识
  ✅ "AI 估算形象"命名和透明标注
  ✅ 子组件跟随父路径状态

═══════════════════════════════════════
V1 候选（POC 通过后实施）
═══════════════════════════════════════

  ⚠️ VTON Provider 选择（PixelAPI / FitRoom / FASHN.ai / 其他）
  ⚠️ Provider Outfit 端点（上+下双件试穿）
  ⚠️ AI 估算形象降级路径（含参数翻译引擎等子组件）
  ⚠️ 前端检测技术方案（MediaPipe / TF.js / 其他）
  ⚠️ 照片轻微修复（亮度归一化/旋转校正）
  ⚠️ 自动抠图（需验证不损害 VTON）
  ⚠️ Negative prompt + 摄影术语
  ⚠️ 简约室内场景实验开关
  ⚠️ BMI 粗分类（低优先级，跟随 AI 估算形象路径）

═══════════════════════════════════════
V1 明确不做
═══════════════════════════════════════

  ❌ 自实现级联 VTON（有 Provider Outfit 端点替代）
  ❌ 3 件及以上衣物试穿
  ❌ 叠穿支持
  ❌ 服装四视图
  ❌ 视频生成
  ❌ 任何自动体型判定
  ❌ 任何无数据支撑的硬数字阈值
  ❌ 锁定具体 VTON 模型或 Provider
  ❌ OpenPose / SMPL / SMPL-X
  ❌ FaceID / InstantID
  ❌ "AI 人物锚定图"术语（已删除）


G：明确删除的旧设计
#	删除项	来源	理由
G1	"不存在成熟商业 Multi-garment VTON API"	第二轮文档	事实错误
G2	MediaPipe 作为锁定实现	第一/二轮文档	能力与实现分离
G3	所有无依据的数字阈值（30°/45°/80%等）	第一/二轮文档	降级为临时参数或定性描述
G4	统一的人物完整性规则	第一/二轮文档	改为按服装类别分区
G5	Provider Success = 结果 Success	第一/二轮文档	拆分为两个维度
G6	"AI 人物锚定图"术语	全部历史文档	重命名为"AI 估算形象"
G7	自实现级联 VTON	第二轮文档	有 Provider Outfit 端点替代
G8	AI 估算形象子组件独立标记	第二轮文档	跟随父路径状态


H：下一阶段应该停止争论、开始执行的事项
#	执行项	说明
H1	POC-01：Provider 注册和对比测试	注册 PixelAPI / FitRoom / FASHN.ai 的免费套餐，用相同输入跑对比。这是最优先的执行项——没有 Provider 选择，所有其他设计都是纸上谈兵
H2	VTON 抽象接口代码设计	基于 V1 已确定的架构（分层接口 + 适配器），开始写接口定义。这不依赖 Provider 选择
H3	人物质量检测的前端实现	能力已确定，先实现硬条件检测（单人/身体完整/分辨率），软评分待前端方案 POC 后加
H4	执行状态 + 质量状态的状态机设计	架构已确定，可直接编码
H5	AI 估算形象路径的延后决策	不争论细节，等 POC-01/02 结果出来后再决定是否实施


三轮累计比分
轮次	ChatGPT 胜	扣子胜	关键修正
第一轮	6	0	AI 锚定图/多件/阈值/BMI/OpenPose/锁模型
第二轮	6	0	硬软分层/重新上传/零偏差/级联VTON/数字/标注
第三轮	10	0	Multi-garment API 存在/能力实现分离/数字分类/按类别判断/脸分级/状态分离/质量检查/命名/子组件/架构Provider分离
累计	22	0	

22:0。三轮全败，没有一次成功反驳。
但三轮辩论的产出是实质性的：
• V1 架构从"什么都想做"收敛到"只做确定能做的"
• 发现了商业 Outfit 端点的存在（第三轮最重要的新事实）
• 建立了能力/实现分离、架构/Provider 分离、执行/质量分离的三层抽象
• 所有无依据的数字被清理或降级
• POC 优先级清晰
辩论到此应该转化为执行。最重要的下一步不是再写文档，是注册 Provider 免费套餐开始跑 POC。

文档日期：2026-08-19
辩论记录：第三轮 12 项争议，ChatGPT 10 胜 / 扣子 0 胜
方案第六次修订
三轮累计：ChatGPT 22 胜 / 扣子 0 胜

本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。