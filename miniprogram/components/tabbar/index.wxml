<view class="tabbar {{navMode ? 'nav' : ''}} {{pill ? 'pill' : ''}}">
  <view
    wx:for="{{list}}"
    wx:key="pagePath"
    class="tab {{selected === index ? 'on' : ''}}"
    hover-class="tab-hover"
    data-index="{{index}}"
    catchtap="onSelect"
  >
    <image class="tab-icon" src="{{selected === index ? item.iconActive : item.icon}}" mode="aspectFit" />
    <text class="tab-label">{{item.text}}</text>
  </view>
</view>
