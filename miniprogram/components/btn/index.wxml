<button
  class="btn {{type}} {{size}} {{disabled || loading ? 'disabled' : ''}}"
  hover-class="btn-hover"
  disabled="{{disabled || loading}}"
  loading="{{loading}}"
  catchtap="onTap"
>
  <slot></slot>
</button>
