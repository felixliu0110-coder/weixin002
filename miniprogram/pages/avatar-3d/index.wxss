.wx-page { height: 100vh; display: flex; flex-direction: column; background: var(--bg); overflow: hidden; }
.avatar-stage { border-radius: var(--radius-lg); overflow: hidden; border: 1rpx solid var(--border-soft); background: var(--surface-2); box-shadow: var(--shadow-card); }
.avatar-img { width: 100%; display: block; }
.example-tag { position: absolute; top: 16rpx; left: 16rpx; background: rgba(31,29,27,0.55); color: #FFFFFF; font-size: 20rpx; padding: 4rpx 14rpx; border-radius: 999rpx; z-index: 2; }
.view-labels { display: flex; justify-content: space-between; padding: 14rpx 40rpx 18rpx; color: var(--muted); font-size: 22rpx; }
.stage-fallback { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 20rpx; min-height: 640rpx; color: var(--fg-2); font-size: 26rpx; }
.stage-fallback .iconfont { font-size: 68rpx; color: var(--fg-2); }
.fb-text { font-size: 26rpx; color: var(--fg-2); margin-top: 16rpx; }
.fb-btn { display: block; width: 60%; margin-top: 28rpx; }
.meas-hint { text-align: center; font-size: 24rpx; color: var(--muted); margin-top: 24rpx; letter-spacing: 0.04em; }

/* 人物资料：section + 数据网格 + 分割线（不用厚卡片） */
.sec-hd { display: flex; align-items: center; justify-content: space-between; margin: 56rpx 4rpx 0; padding-bottom: 20rpx; border-bottom: 1rpx solid var(--border-soft); }
.sec-title { font-size: 34rpx; font-weight: 600; }
.more { display: flex; align-items: center; gap: 4rpx; font-size: 26rpx; color: var(--accent-text); padding: 8rpx 4rpx; }
.more .iconfont { font-size: 26rpx; }
.more-hover { opacity: 0.8; }
.profile-grid { display: grid; grid-template-columns: repeat(3, 1fr); }
.p-cell { padding: 28rpx 16rpx 24rpx; text-align: center; }
.pc-label { font-size: 24rpx; color: var(--fg-2); letter-spacing: 0.02em; }
.pc-val { display: block; font-size: 34rpx; font-weight: 700; color: var(--fg); margin-top: 10rpx; }
.pc-val small { font-size: 22rpx; font-weight: 500; color: var(--fg-2); margin-left: 2rpx; }
.footer-bar { display: flex; gap: 20rpx; padding-bottom: env(safe-area-inset-bottom); }
.footer-main { flex: 2; }
.footer-sub { flex: 1; }
