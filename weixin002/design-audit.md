# 我形我衣 · 全量设计诊断报告

> Creative Director 诊断轮 · 只诊断、未改码。
> 范围：17 屏 + 预览器 + 共享设计系统。分级：P0 阻塞 / P1 应修 / P2 加分。

## 执行状态

- **B1（P1 一致性）已完成**：S1 TabBar 对比、S2 说明文字对比、S3 分段选中态、06 空态、07 meta 两行、11 按钮文案、14 提示与按钮主次、09 遮罩关闭，以及 02/03/04 文案精简、06 字号、预览器小屏适配；样式版本升至 v=15。
- **B2（素材/内容）部分完成**：07/13 已确认采用 AI 生成全身示意图，但运行环境未配置媒体服务密钥，**图片生成服务暂时不可用**，暂保留原图素材并在界面标注“示意 / 仅供参考”；08、10、12 的“示例”标注已完成。
- **B3（P2 细节）已完成**：16 提交内联成功态、13 页脚说明、文案统一。
- **交付物**：新增 `woxingwoyi-demo.html`（17 屏合一演示界面）。

## 一、系统级问题（跨屏）

| # | 级别 | 问题 | 建议 |
|---|---|---|---|
| S1 | P1 | TabBar 选中态文字/图标用 `--accent-deep`（#C98F80）在白底 11px，对比约 2.7:1，低于 4.5:1 | 选中态改用深色 mix（同 05 按钮图标的处理），保持品牌粉色但加深 |
| S2 | P1 | `.hint`（12.5px `--muted` #8F8378）在米白底约 3:1，作为说明文字不达 4.5:1 | 说明文字提为 `--fg-2`；仅纯装饰性注释可用 muted |
| S3 | P1 | 分段控件（02 性别）选中态是“白底+阴影”，与全站粉色强调体系不一致 | 选中态统一为 accent-soft 底 + accent-deep 字 |
| S4 | P2 | 多页存在“演示数据”未标注（08 badge 12、10/12 排队位、03 三围数值） | 在界面或 README 标注“示例”；08 badge 弱化 |
| S5 | P2 | 部分说明文案偏长（04、07、16） | 精简为 1–2 行 |

已达标项：focus-visible 全局、触控目标 ≥44px、reduced-motion 全局、无裸色值溢出 token、按钮 hover 不变灰。

## 二、逐屏清单

### 01 登录
- 已修：单一标题、副标题上移、协议一行、插画无黄绿带、无多余搜索。✓
- P2：登录按钮 hover 与全站主按钮 hover 一致（亮度微升）。

### 02 基本信息
- P1：性别分段选中态与品牌强调体系不一致（见 S3）。
- P2：副标题“约 1 分钟完成，后续可随时修改”可精简为“后续可随时修改”。

### 03 身材参数
- P1：信息密度偏高（三围卡 + 开关 + 2 滑块 + 4 标签 chip 同屏）；chip 行在 ≤360px 会拥挤 | 分组卡片化、chip 2×2 或缩小字号。
- P2：三围数值为 PRD 示例补齐，界面未标注“示例” | 在“三围”标题旁加“示例”小标。

### 04 照片上传
- P2：step-note 三行说明可精简为两行（“不上传使用默认形象；中途自动保存”）。
- 已达标：双卡 + 单一主按钮、hover/focus 态。

### 05 我的数字人
- 已修：按钮 64px 粉色系、身材档案卡片、38px 间距、图标对比度、图片加载兜底。✓
- P2：旋转/标注/确认为 toast 示意，与真实 3D 交互差距，原型说明即可。

### 06 选择衣物
- P1：**缺空态**：模板库为空时无引导文案 | 补“暂无模板衣物”空态 + 上传入口。
- P2：卡片名称 12px / 分类 10.5px 偏小 | 提至 12.5 / 11px。

### 07 试穿结果
- P1：素材方案未定（3/4 构图 vs 全身像）——见“待决策项”。
- P1：meta 说明句偏长 | 拆为“按你的身材（168cm / 55kg）生成” + “尺码建议仅供参考”两行。
- P2：角度切换仅 toast，无视觉切换（原型说明即可）。

### 08 个人中心
- P1：试穿记录 badge“12”为演示数据 | 标注示例或弱化。
- P2：设置齿轮为占位，可保留。

### 09 隐私授权
- P2：弹窗无遮罩点击关闭 | 增加点遮罩返回（配合现有不同意按钮）。
- 已达标：按钮对比、文案与 PRD C-03 对齐。

### 10 / 12 生成进度
- P2：排队位“第 1/2 位”为演示值 | 标注示例。
- 已达标：进度环动画 + 自动跳转、文案一致。

### 11 衣物预览
- P1：主按钮文案“确认已生成试穿”拗口 | 改为“确认生成试穿”。
- P2：抠图角标位置已统一。

### 13 试穿记录
- P1：图片为半身/平铺混合（受原图限制），与“全身像”目标不一致——见“待决策项”。
- P2：2×2+1 布局末尾单卡左对齐 | 可接受，或调整为 4 卡。

### 14 对比视图
- P1：提示“选择左侧保存”语义不清 | 改为“点圆点切换，保存所选”。
- P2：保存/分享同为次级按钮，主次不清 | 建议“保存”作主按钮、分享次级。

### 15 隐私与数据管理
- 已达标：删除二次确认、danger 主按钮。
- P2：行内副标题 12px 可读性可微调。

### 16 反馈与关于
- P1：textarea 已有 label 关联 ✓；提交仅 toast | 可加提交成功的内联状态（P2）。
- P2：关于段三行正文可精简。

### 17 首页
- 已修：导航搜索图标移除、头像换 p05、按钮居中、模板命名规范。✓
- P2：一键试穿（主卡）与数字人卡“去试穿”并存，数字人卡按钮已为次级样式，符合主次关系。

### 预览器 index.html
- 已修：去光晕、圆点可键盘操作、加载态、自动演示选中态、reduced-motion。✓
- P1：PRD 映射弹层表格在小屏需检查横向滚动。

## 三、可访问性汇总

- 达标：focus-visible 全组件、触控 ≥44px、`prefers-reduced-motion`、语义结构（header/nav/main/button）、图片 alt、图标按钮 aria-label。
- 待修：S1 TabBar 文字对比、S2 说明文字对比。
- 平台：Android 48dp 触控目标未逐屏核对（本轮收尾时检查）。

## 四、素材与文案

- 07 / 13 效果图构图方案未定：保持原图 3/4 / AI 生成全身示意 / 用户提供素材。该决策影响视觉说服力，但不阻塞其余修复，可延后。
- 文案总体已去空泛；待精简点见 S5 与 02 / 04 / 07 / 16。

## 五、建议执行批次

- **B1（P1 一致性，先做）**：S1 TabBar 对比、S2 hint 对比、S3 分段选中态、11 按钮文案、07 meta 精简、06 空态、14 提示与按钮主次、09 遮罩关闭。
- **B2（素材/内容，需决策后做）**：07 / 13 素材方案；03/08/10/12“示例”标注。
- **B3（P2 细节）**：02/04/16 文案精简、06 字号、13 布局、16 提交状态、预览器表格小屏适配。

## 待你确认

1. 是否按 B1 → B2 → B3 执行？（默认：本轮先做 B1，全部 P1 一次到位）
2. 07 / 13 素材方案可在 B2 前再定，不阻塞。
